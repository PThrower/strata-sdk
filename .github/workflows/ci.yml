name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - name: Build SDK first (action typecheck needs its types)
        run: npm run build --workspace @strata-ai/sdk
      - name: Type-check
        run: npm run typecheck
      - name: Build (remaining packages)
        run: npm run build
      - name: Test
        run: npm test

  action-dist-drift:
    runs-on: ubuntu-latest
    name: Action dist must match source
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - name: Build SDK
        run: npm run build --workspace @strata-ai/sdk
      - name: Build action ncc bundle
        run: npm run build --workspace @strata-ai/action
      - name: Verify dist/ is up to date
        run: |
          if ! git diff --quiet packages/action/dist/index.js; then
            echo "::error::packages/action/dist/index.js drifted from source — run 'npm run build --workspace @strata-ai/action' and commit"
            git diff packages/action/dist/index.js | head -20
            exit 1
          fi
