{
  "name": "@strata-ai/action",
  "version": "1.0.0",
  "private": true,
  "description": "GitHub Action for Strata MCP Security Check — finds MCP server references in your repo and verifies each one.",
  "license": "MIT",
  "author": "Strata <support@strata.dev>",
  "type": "module",
  "scripts": {
    "build": "ncc build src/main.ts -o dist --source-map --license licenses.txt",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@strata-ai/sdk": "*",
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@vercel/ncc": "^0.38.2",
    "typescript": "^5.6.2"
  }
}
